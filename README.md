# Address Location
```
CREATE TABLE Address (
    AddressID INT PRIMARY KEY,
    Building VARCHAR(100),
    Level VARCHAR(50),
    Room VARCHAR(50),
    Carpark VARCHAR(50),
    Lobby VARCHAR(50),
    Pantry VARCHAR(50)
);
```
